{{ django_settings }}

## <components>
#
# Below are settings generated by fuse.
#
# Fuse will only read and manipulate content between the row containing the
# tag <fuse> and the row containing the tag </fuse>. If the file contain
# duplicate openings or closing tags, fuse will choose the first opening
# tag and the last closing tag (greedy).
#
# Sections are formatted according to following pattern.
# 
# ## Section section-name (section-hash)
#
# # Section Description
# SOME_KEY = SOME_VALUE
# 
# ## End section section-name (section-hash)
# 

{% for section_identifier in sections %}
{{ section.hash }}## Section {{ section.name }}

{% for key in settings %}
# {{ settings[key].description }}
{{ key }} = {{ settings[key].value }}
{% endfor %}

## End section {{ section.name }}{{ section.hash }}
{{ sections[section_identifier] }}
{% endfor %}

#
# This is the closing tag for fuse. Anything below this tag will be ignored.
# 
## </components>
